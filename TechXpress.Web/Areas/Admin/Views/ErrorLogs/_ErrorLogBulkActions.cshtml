<div class="row mt-3">
    <div class="col-md-6">
        <div class="d-flex align-items-center">
            <button class="btn btn-danger me-2" onclick="ErrorLog.confirmBulkDelete()" id="bulkDeleteBtn" disabled>
                <i class="fas fa-trash me-2"></i>Delete Selected
            </button>
            <span class="text-muted" id="selectedCount">0 selected</span>
        </div>
    </div>
</div>